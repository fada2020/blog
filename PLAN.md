# AI Astro Blog Automation Platform

## 1. 목표

기존 Obsidian 업무일지에서 공개 가치가 있는 기술 주제를 추출하고,
민감정보를 제거한 기술 블로그 글로 재구성하여 Astro 정적 사이트에 발행한다.

초기에는 사람이 모든 초안을 검토하고 승인한다.
수동 승인 글 30개를 운영한 뒤 조건부 자동 발행 전환 여부를 다시 결정한다.

## 2. 확정된 운영 정책

### 콘텐츠 소스

- 기본 입력은 `~/obsidian/folder1/curestream/00_업무일지/1.daily/*.md`다.
- 업무일지에 연결된 `04_장애이력` 문서는 기술적 근거를 보강할 때만 읽는다.
- 주간 업무일지, 템플릿, 첨부파일과 빈 업무일지는 수집하지 않는다.
- 최초 실행 시 최근 2개월을 백로그로 수집한다.
- 오래된 작업 에피소드부터 후보로 처리한다.
- 백로그 소진 후 최신 업무일지를 같은 방식으로 처리한다.

### AI 실행

- 작성과 검토에는 로컬에 로그인된 Codex CLI를 사용한다.
- OpenAI API와 별도 API 키는 초기 범위에 포함하지 않는다.
- Codex CLI에는 업무일지 읽기와 블로그 저장소 쓰기에 필요한 최소 경로만 제공한다.

### 저장소와 배포

- 저장소는 `fada2020/blog`를 사용한다.
- GitHub Pages 프로젝트 사이트로 배포한다.
- 공개 주소는 `https://fada2020.github.io/blog/`다.
- Astro 설정은 `site: "https://fada2020.github.io"`, `base: "/blog"`를 사용한다.
- 커스텀 도메인은 초기 범위에서 제외한다.

## 3. 주간 편성

대한민국 영업일을 기준으로 하루 최대 한 편만 발행한다.
토요일, 일요일, 법정공휴일과 대체공휴일에는 발행하지 않는다.

### 기본 편성

| 영업일 순서 | 콘텐츠 |
|---|---|
| 첫 번째 | 순차 학습 트랙 |
| 두 번째 | 최근 7일 기술 동향 |
| 중간 | 업무일지 기반 실무 회고 |
| 마지막 | 해당 주 기술 심화 |

### 짧은 주

| 영업일 수 | 발행 구성 |
|---:|---|
| 5일 | 학습, 기술 동향, 실무 회고, 실무 회고, 주간 심화 |
| 4일 | 학습, 기술 동향, 실무 회고, 주간 심화 |
| 3일 | 학습, 기술 동향, 주간 심화 |
| 2일 | 학습, 기술 동향 |
| 1일 | 학습 |

- 승인된 글이 없으면 해당 날짜에는 발행하지 않는다.
- 공휴일에 발행하지 못한 글을 다음 날 몰아서 발행하지 않는다.
- 발행하지 않은 후보는 대기열 순서를 유지한다.

## 4. 순차 학습 트랙

학습 트랙은 다음 순서로 진행한다.

```text
Next.js -> React Native -> Kotlin -> Flutter
```

- 각 트랙의 계획된 커리큘럼을 완료한 뒤 다음 트랙으로 이동한다.
- 중요한 신규 기능은 학습 순서를 되돌리지 않고 기술 동향 또는 보충편에서 다룬다.
- Flutter 이후의 학습 트랙은 미리 정하지 않는다.

### 독자 기준

- Java와 Spring Boot 경험이 있는 개발자를 기본 독자로 가정한다.
- 새 개념은 Java, Spring MVC, Spring Security, JPA 등 익숙한 개념과 비교한다.
- 단순 문법 소개보다 역할, 생명주기와 설계 차이를 설명한다.
- 최소 실행 예제와 직접 풀 수 있는 연습 문제를 포함한다.

### 학습 글 구조

```text
이번 주 개념
-> Java/Spring에서는 어떻게 다뤘는가
-> 대상 기술에서는 무엇이 다른가
-> 대응 개념 비교
-> 최소 실행 예제
-> Java 개발자가 자주 하는 오해
-> 연습 문제
```

## 5. 기술 동향

- 최근 7일 안에 공식 발표된 내용만 사용한다.
- 공식 문서, 릴리스 노트와 공식 GitHub 저장소를 우선한다.
- 루머와 출처가 불분명한 내용은 제외한다.
- 버전, 발표일, 지원 상태, 실무 영향과 적용 필요 여부를 명시한다.
- 중요한 소식이 없으면 개수를 억지로 채우지 않는다.

주제 비중은 다음을 기본으로 한다.

- 70%: Kotlin, Java, Spring Boot, Next.js, AWS, Docker, Terraform, PostgreSQL,
  Redis, Elasticsearch와 Grafana 등 현재 사용하는 기술
- 20%: React, TypeScript, CI/CD, OpenTelemetry, 보안과 데이터베이스 등 인접 기술
- 10%: 새롭게 주목할 런타임, 개발 도구와 AI 개발 워크플로우

## 6. 업무일지 기반 실무 회고

### 주제 추출

1. Markdown에서 날짜, 태그, 번호별 섹션과 위키링크를 구조적으로 추출한다.
2. 회의, 일정, 행정, 단순 상태 확인과 기술 설명이 없는 항목을 제외한다.
3. Codex CLI가 각 섹션의 기술 주제, 문제, 결정, 구현과 검증을 판정한다.
4. 동일한 시스템, 문제와 설계 변경에 속한 기록을 작업 에피소드로 묶는다.
5. 마지막 관련 기록에서 7일 이상 간격이 생기면 새 에피소드로 분리한다.
6. 합친 이유와 모든 원문 출처를 후보 파일에 남긴다.

### 글 구성

업무일지를 날짜순으로 옮기지 않고 다음 서사로 재구성한다.

```text
문제
-> 기존 설계의 한계
-> 선택한 설계
-> 구현
-> 시행착오
-> 검증
-> 배운 점
```

한 날짜에 독립적인 기술 주제가 여러 개면 서로 다른 후보로 분리한다.
여러 날짜에 걸친 동일한 설계 변경과 검증은 한 편으로 통합한다.

## 7. 주간 기술 심화

- 해당 주에 실제 발행된 글에서 학습 가치가 높은 기술 하나를 선택한다.
- 앞선 글을 요약하지 않고 원리와 선택 기준을 더 깊게 설명한다.
- 공식 문서와 검증 가능한 기술 근거를 사용한다.
- 내부 사례는 식별정보를 제거한 배경 예시로만 사용한다.
- 독립 실행 가능한 실습과 해당 주 관련 글 링크를 포함한다.

```text
이번 주에 이 기술이 중요했던 이유
-> 핵심 개념
-> 내부 동작 원리
-> 실무 설계 선택지
-> 흔한 실패 사례
-> 독립 실행 실습
-> 이번 주 관련 글
```

영업일이 2일 이하인 주에는 주간 심화 글을 만들지 않는다.

## 8. 문체

기본 문체는 존댓말 기반의 솔직한 실무 회고형이다.

- `~했습니다`, `~확인했습니다` 형태를 사용한다.
- 문제를 처음부터 정확히 알았던 것처럼 꾸미지 않는다.
- 실패한 접근과 잘못 판단한 부분도 간결하게 기록한다.
- 과장된 제목과 `완벽한`, `혁신적인`, `쉽게` 같은 표현을 피한다.
- 결론을 먼저 말하고 근거를 뒤에 설명한다.
- 내부 시스템 자랑보다 다른 개발자가 재사용할 수 있는 교훈을 우선한다.
- 독자에게 말을 거는 표현은 최소화한다.

## 9. 이미지와 다이어그램

- 모든 공개 글에는 `1200x630` 대표 이미지를 한 장 제공한다.
- 본문 이미지는 기술 이해에 도움이 될 때만 추가한다.
- 구조, 상태 전이와 데이터 흐름은 수정 가능한 Mermaid 다이어그램을 우선한다.
- UI와 실행 결과가 중요하면 직접 만든 예제의 스크린샷을 사용한다.
- 개념 설명에는 직접 생성한 이미지를 사용할 수 있다.
- 외부 이미지는 공식 출처와 사용 조건을 확인한 경우에만 저장소에 내려받아 사용한다.
- 외부 URL을 그대로 hotlink하지 않는다.
- 모든 이미지에 구체적인 대체 텍스트를 작성한다.
- 외부 자료에는 출처 이름과 원문 URL을 함께 저장한다.
- Phase 1은 이미지 표시, 최적화, 출처와 Mermaid 렌더링을 지원한다.
- Phase 2에서 Codex CLI가 글과 함께 대표 이미지 프롬프트와 Mermaid 초안을 생성한다.

이미지 파일은 글 단위 디렉터리로 관리한다.

```text
src/assets/blog/
  event-ordering/
    cover.webp
    architecture.webp
    verification-flow.webp
```

## 10. 개인정보와 기밀 보호

익명화는 발행 전 필수 단계다.

다음 항목은 제거하거나 일반화한다.

- 실제 조직, 회사, 고객사와 서비스 이름
- 개인 이름, 이메일, 계정명과 연락처
- 환자, 사용자와 관리자 식별 정보
- 실제 도메인, IP, 내부 URL과 네트워크 구조
- AWS 계정 번호, 인스턴스 ID, ARN과 기타 리소스 ID
- 토큰, 비밀번호, 키와 환경변수 값
- 비공개 저장소 이름과 로컬 경로
- 실제 장애 로그의 추적 ID와 식별 가능한 메타데이터

익명화 여부가 불확실하면 승인을 차단하고 검토 대기로 보낸다.
실제 프로젝트 코드는 게시하지 않고 핵심 원리를 재현한 독립 샘플을 작성한다.

## 11. 승인과 발행

별도 관리자 화면 없이 로컬 CLI를 사용한다.

```bash
./blog list
./blog approve <slug>
./blog reject <slug>
```

- 생성 결과는 항상 `draft: true`로 저장한다.
- `approve`가 실행된 글만 `draft: false`로 변경한다.
- 승인 명령이 Git commit과 push를 수행한다.
- GitHub Actions가 승인된 커밋을 빌드하여 GitHub Pages에 배포한다.
- 글 생성 성공만으로 커밋하거나 푸시하지 않는다.

## 12. 파일 기반 상태

초기에는 데이터베이스와 관리 서버를 만들지 않는다.
Git에 저장된 파일을 단일 진실 공급원으로 사용한다.

```text
config/
  editorial.yml
  curriculum.yml

data/
  candidates/
  reviews/
  publication-log.yml

src/content/blog/
  *.mdx
```

## 13. 실패 처리

- Codex CLI 실행 실패 시 커리큘럼 진도와 후보 대기열을 변경하지 않는다.
- 기술 동향의 공식 근거가 부족하면 해당 글을 발행하지 않는다.
- 외부 이미지의 출처와 사용 조건을 확인할 수 없으면 해당 이미지를 사용하지 않는다.
- 이미지 생성이 실패하면 대표 이미지 없는 글을 자동 승인하지 않는다.
- 익명화 검사에 실패한 글은 승인할 수 없다.
- 승인된 글이 없으면 빈 글을 만들지 않는다.
- 배포 실패 시 발행 이력을 성공으로 기록하지 않는다.
- 같은 원문과 에피소드로 중복 후보를 생성하지 않는다.

## 14. 검증

- 대한민국 영업일과 짧은 주 편성 테스트
- 업무일지 Markdown 파싱 테스트
- 여러 날짜의 주제 통합과 7일 경계 테스트
- 개인정보, 도메인, AWS 식별자 차단 테스트
- 승인되지 않은 글의 빌드 제외 테스트
- `/blog/` 하위 링크, RSS와 Sitemap 검증
- 학습 트랙 진도 전환 테스트
- 기술 동향의 출처와 발표일 검증
- 대표 이미지 크기, 대체 텍스트와 출처 검증
- Mermaid 다이어그램의 빌드와 모바일 렌더링 검증

## 15. 개발 단계

### Phase 1: Astro 블로그와 수동 발행

- Astro, TypeScript와 MDX
- Content Collections
- 글 목록, 상세, 카테고리와 태그
- 검색, RSS, Sitemap, SEO Meta와 Dark Mode
- 대표 이미지, 본문 이미지, 출처 표시와 Mermaid 다이어그램
- GitHub Pages `/blog/` 배포
- `draft` 글 배포 제외

### Phase 2: 업무일지 기반 승인형 생성

- 최근 2개월 업무일지 수집
- 혼합형 주제 추출과 에피소드 통합
- 민감정보 검사와 일반화
- Codex CLI 초안 생성과 기술 검토
- 대표 이미지 프롬프트와 Mermaid 다이어그램 초안 생성
- 로컬 승인 명령

### Phase 3: 편성 및 품질 검증

- 대한민국 영업일 편성
- 순차 학습 트랙
- 최근 기술 동향
- 주간 기술 심화
- 코드 예제 자동 검증과 중복 탐지

### Phase 4: 제한적 자동 발행

- 수동 승인 글 30개 운영 결과 검토
- 자동 발행 전환 여부 재결정
- 품질 기준 미달 글은 계속 검토 대기로 전환

## 16. 현재 상태

- 핵심 운영 정책과 편성 설계 완료
- 구현과 프로젝트 스캐폴딩은 시작하지 않음
- 다음 단계는 구현 계획 작성과 Phase 1 착수
